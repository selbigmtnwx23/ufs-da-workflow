geometry:
  geom_grid_file: ./soca_gridspec.nc
  mom6_input_nml: ./input.nml
  fields metadata: ./fields_metadata.yaml
background:
  read_from_file: 1
  basename: {{ soca_background_basename }}/
  ocn_filename: {{ soca_background_filename_ocn }}
  ice_filename: {{ soca_background_filename_ice }}
  date: &date {{ soca_background_date_iso }}
  state variables:
  - sea_surface_height_above_geoid
background error:
  covariance model: SABER
  saber central block:
    saber block name: diffusion
    geometry:
      geom_grid_file: ./soca_gridspec.nc
      mom6_input_nml: ./input.nml
      fields metadata: ./fields_metadata.yaml
    calibration:
      normalization:
        method: randomization
        iterations: 1000
      groups:
      - horizontal:
          model file:
            date: *date
            basename: ./
            ocn_filename: {{ soca_bkg_error_hz_ocn_fn }}
          model variable: sea_surface_height_above_geoid
        write:
          filepath: ./staticb/{{ soca_diff_cor_hz_fn }}
